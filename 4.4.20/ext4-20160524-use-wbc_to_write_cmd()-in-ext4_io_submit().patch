
Use wbt-provided wbc_to_write_cmd() to match xfs_aops.c:xfs_submit_ioend_bio().
No functional change.

Signed-off-by: Holger Hoffstätte <holger@applied-asynchrony.com>

--- ./fs/ext4/page-io.c~	2016-05-24 23:34:31.000000000 +0200
+++ ./fs/ext4/page-io.c	2016-05-24 23:40:24.174941243 +0200
@@ -354,9 +354,7 @@ void ext4_io_submit(struct ext4_io_submi
 	struct bio *bio = io->io_bio;
 
 	if (bio) {
-		int io_op = io->io_wbc->sync_mode == WB_SYNC_ALL ?
-			    WRITE_SYNC : WRITE;
-		submit_bio(io_op, io->io_bio);
+		submit_bio(wbc_to_write_cmd(io->io_wbc), io->io_bio);
 	}
 	io->io_bio = NULL;
 }
