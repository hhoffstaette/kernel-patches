
Remove a WARN_ON_ONCE that was deemed a false positive.
See the following mailing list thread for discussion:
https://groups.google.com/forum/?hl=en#!topic/bbr-dev/5qe_ARx4s4s

Signed-off-by: Holger Hoffstätte <holger@applied-asynchrony.com>

diff -rup a/net/ipv4/tcp_bbr2.c b/net/ipv4/tcp_bbr2.c
--- a/net/ipv4/tcp_bbr2.c	2019-10-02 17:15:47.163476792 +0200
+++ b/net/ipv4/tcp_bbr2.c	2019-10-02 17:16:58.870762226 +0200
@@ -2424,7 +2424,6 @@ static void bbr_set_state(struct sock *s
 		bbr_debug(sk, 0, &rs, &dbg);
 	} else if (bbr->prev_ca_state == TCP_CA_Loss &&
 		   new_state != TCP_CA_Loss) {
-		WARN_ON_ONCE(bbr->prior_cwnd == 0);
 		WARN_ON_ONCE(bbr->prior_cwnd == ~0U);
 		tp->snd_cwnd = max(tp->snd_cwnd, bbr->prior_cwnd);
 	}
